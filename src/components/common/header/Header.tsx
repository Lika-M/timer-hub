import './Header.scss';

export default function Header() {

    return (
        <header className="header">
            <h2>
                <span>Time Management </span>
                <span>Techniques for</span>
                <span> Busy People</span>
            </h2>
        </header>
    );
}